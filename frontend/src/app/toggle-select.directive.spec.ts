import { ToggleSelectDirective } from './toggle-select.directive';

describe('ToggleSelectDirective', () => {
  it('should create an instance', () => {
    const directive = new ToggleSelectDirective();
    expect(directive).toBeTruthy();
  });
});
